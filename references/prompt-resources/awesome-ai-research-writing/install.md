# Install awesome-ai-research-writing

This resource is not installed by default. It is tracked as `reference-only` in `inventory/resources.yaml`. Use `bin/ai-research-toolkit sync --id awesome-ai-research-writing` only when the resource is eligible and explicitly needed. Do not use sudo.
