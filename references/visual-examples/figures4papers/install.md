# Install figures4papers

This resource is not installed by default. It is tracked as `reference-only` in `inventory/resources.yaml`. Use `bin/ai-research-toolkit sync --id figures4papers` only when the resource is eligible and explicitly needed. Do not use sudo.
