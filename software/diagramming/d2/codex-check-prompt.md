# Codex check prompt for D2

Use this prompt when asking Codex to validate a D2 installation or rendering path:

```text
Check D2 without installing anything. Run `d2 --version`, render a minimal .d2 file to SVG under smoke/d2/, and report whether the SVG was actually created. Do not scan project data directories and do not use sudo.
```
