# Install diagrams.py

Install status: unknown.

User-level Python environment option:

```bash
python -m venv envs/diagrams-venv
. envs/diagrams-venv/bin/activate
pip install diagrams
python -c "import diagrams"
```

Graphviz is required for rendering.
