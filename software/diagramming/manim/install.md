# Install Manim

Install status: unknown.

User-level Python environment option:

```bash
python -m venv envs/manim-venv
. envs/manim-venv/bin/activate
pip install manim
manim --version
```

Rendering videos may require ffmpeg, Cairo, LaTeX, and fonts on headless Linux.
