# Notes for Mermaid CLI

Resource id: `mermaid-cli`.

Role: Mermaid 图表的命令行渲染器，适合从 Markdown/文档中的流程图、时序图、状态图生成 SVG/PNG/PDF。

Lifecycle and integration status are maintained in `inventory/resources.yaml`; do not duplicate them here.
