# Install PlantUML

Install status: unknown.

User-level options, no sudo:

```bash
# If plantuml is already available:
plantuml -version

# Or keep a manually downloaded plantuml.jar under bin/ after checksum review:
java -jar bin/plantuml.jar -version
```

PlantUML may use Graphviz for some diagram types.
