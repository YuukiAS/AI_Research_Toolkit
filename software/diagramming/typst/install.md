# Install Typst

Install status: unknown.

User-level options, no sudo:

```bash
# Prefer a user-managed binary in bin/ or a package manager that writes under envs/.
typst --version
```

TODO: inspect upstream README for the preferred local installation path.
