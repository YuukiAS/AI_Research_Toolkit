# Crafter

## Role
科研图生成或编辑工具占位资源；当前只有资源名，没有确认 upstream URL。

## Upstream
not specified

## Local layout
`repo/` 是 upstream clone 或预留位置；外层 `README.md`、`install.md`、`notes.md`、`privacy.md`、`examples/` 是本 toolkit 的本地说明和示例，不写入 upstream repo 根目录。

## Install status
unknown

## Typical usage
- `TODO: identify upstream repository`

## Privacy and safety
未确认工具行为前，不应导入敏感论文、数据或项目目录。
